export enum ROUTE_PATH {
    home = '/',
    generator = '/generator',
    firstStep = '/first-step'
}

export enum ROUTE_NAME {
    home = 'home',
    generator = 'generator',
    firstStep = 'first-step'
}